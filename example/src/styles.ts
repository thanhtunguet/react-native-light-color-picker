import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  box: {
    width: 60,
    height: 60,
    marginVertical: 20,
  },
  demo: {
    width: '100%',
    height: 80,
    marginTop: 20,
    zIndex: 0,
    justifyContent: 'center',
    alignItems: 'center',
  },
  tab: {
    width: '100%',
    flex: 1,
  },
});
